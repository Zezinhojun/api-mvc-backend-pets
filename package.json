{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "npm run db:dev:restart && wait-on tcp:5432 && npm run start:dev",
    "start:dev": "nodemon index.js",
    "db:dev:up": "docker-compose up -d db",
    "db:dev:rm": "docker-compose rm -s -f -v db",
    "db:dev:restart": "npm run db:dev:rm && npm run db:dev:up",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "nodemon": "^3.1.0",
    "npm-run-all": "^4.1.5",
    "pg": "^8.11.5",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.3",
    "wait-on": "^7.2.0"
  }
}
